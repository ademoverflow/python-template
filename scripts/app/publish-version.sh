#!/usr/bin/env bash

PACKAGE_NAME=python-template

# TODO: publish docker image python-template:production to our AWS registry with the latest known version (in git).
echo "Nothing to do here (for now).